# 车-臂联调：机械臂侧协议要求

机械臂侧只需配合做一件事：**新增发布 `/arm_task_report`**，并接收两个触发话题。其余用你们现有管线，零改动。

---

## 一、通信协议

### 你们接收（我们发）

| 话题 | 类型 | 含义 |
|---|---|---|
| `/arm/task_command` | `std_msgs/Int32` | 值=货架层号(1/2/3)，收到后执行 对准→检测→抓取→回home |
| `/arm/release_command` | `std_msgs/Empty` | 收到后执行 放置→回home |

### 你们发布（新增，唯一要写的代码）

```text
话题：/arm_task_report
类型：std_msgs/String（JSON）
```

消息格式：

```json
{"command": "STOW|PICK|PLACE", "success": true, "error_code": 0, "message": "说明"}
```

发布时机：

| command | 时机 | success=true 含义 |
|---|---|---|
| `STOW` | 上电回home后周期性发（建议每3秒） | 臂已收拢，底盘可动 |
| `PICK` | 抓稳+已回home后发一次 | 抓稳且臂已收回 |
| `PLACE` | 放下+已回home后发一次 | 已释放且臂已收回 |

错误码：`0成功 3找不到物体 5抓取失败 7超时 8取消 9硬件`

> 硬性要求：PICK/PLACE 成功时机械臂**必须已收回**，否则底盘会撞。

---

## 二、流程

```text
上电 → 回home → 周期性发 STOW 上报

收到 /arm/task_command(层号)
→ 对准+检测+抓取+回home
→ 发 /arm_task_report {command:"PICK", success:true}     ← 抓完停住，别自动放

（底盘导航去送货区）

收到 /arm/release_command
→ 放置+回home
→ 发 /arm_task_report {command:"PLACE", success:true}

循环以上取/放
```

---

## 三、你们负责的（我们不管）

YOLO识别、Aruco精定位、IK轨迹、夹爪、`/place_pose`计算、放置姿态。

---

## 四、自测

```bash
# 桥接在等你们
ros2 node list | grep arm_task_bridge

# 发一条测试上报
ros2 topic pub --once /arm_task_report std_msgs/msg/String \
  "data: '{\"command\":\"STOW\",\"success\":true,\"error_code\":0,\"message\":\"test\"}'"
```

---

## 五、需确认

1. 话题名/JSON 格式 OK？
2. STOW 能周期发吗？（上电发一次也可，桥接有缓存）
3. 你们整套机械臂启动命令？
4. 放货区 `place_planner` 能正常出 `/place_pose` 吗？
