# 车臂实机联调 · 待改动方案

> 状态：方案汇总，**尚未改动任何代码**。
> 关联：`机械臂接口确认(1).md`（话题侧契约） / 车项目 `/home/s1/ros2_ws_1`

---

## 0. 协议核对结论（已对照车项目源码）

车项目实际链路：

```text
competition_node(调度)
   → /arm_task Action  (task_interfaces/action/ArmTask)
   → arm_task_bridge   把 Action 翻译成话题
   → 话题侧协议（= 机械臂接口确认(1).md）
```

- 车侧已实现：`arm_task_bridge.py`（Action⇄话题翻译）、`arm_worker_stub.py`（假臂，联调用，`use_stub=true`）。
- 机械臂**不需要实现 Action server**，只需做**话题侧**：

| 方向 | 话题 | 类型 | 现状 |
|---|---|---|---|
| 收 | `/arm/task_command` | Int32 = 货架层 1/2/3 | ✅ shelf_workflow 已订阅、语义一致 |
| 收 | `/arm/release_command` | Empty | ✅ shelf_workflow 已订阅 |
| 收 | `/arm/shelf/preset_home` | Empty | ✅ 已有（桥接当前未实际发） |
| **发** | **`/arm_task_report`** | std_msgs/String(JSON) | ❌ **待新增（唯一缺口）** |

- 错误码约定：`0 成功 / 3 找不到物体 / 5 抓取失败 / 7 超时 / 8 取消 / 9 硬件`（桥接另加 `2 非法目标`）。
- **硬性约束：PICK / PLACE 上报成功 = 臂必须已收回**（否则底盘直接导航会撞）。

---

## 1. 待改动 A：`shelf_workflow_node` 新增 `/arm_task_report` 上报

### 1.1 话题定义
- 绝对话题 `/arm_task_report`（我们节点在 `/arm` ns，发布器必须用 `/` 开头绝对名）。
- `std_msgs/String`，JSON：`{"command": "STOW|PICK|PLACE", "success": bool, "error_code": int, "message": str}`。
- 建议 topic 做成参数（默认 `/arm_task_report`），方便按现场改。

### 1.2 上报时机
| 事件 | 触发条件（workflow 状态） | 上报 |
|---|---|---|
| STOW | `IDLE`（任务未运行/已结束）周期发，建议每 3 s | `{STOW, success:true}` |
| PICK 成功 | GRASPING 见 executor=`WAIT_RELEASE`（已回 home）且 `grasp_result==2` | `{PICK, true}` 一次，随后进 `WAIT_RELEASE_CMD` 等 release_command |
| PICK 失败 | 最终失败、臂收回完成（`RETURN_HOME`→`IDLE`） | `{PICK, false, code}` 一次 |
| PLACE 成功 | PLACING 完成、executor 回 home → `IDLE` | `{PLACE, true}` 一次 |
| PLACE 失败 | place 超时/中止、收臂后 | `{PLACE, false, code}` 一次 |

### 1.3 空抓当失败（顺带修复现有误判）
- 现状：executor 空抓发 `grasp_result=1`，workflow 将其当成功 → 会**空放**且误报 PICK 成功。
- 改法：`grasp_result==1` 归入失败处理（与 0 同路：`_handle_grasp_failure`→重试→最终失败上报）。
- **失败码定为 `5 抓取失败`（已确认）**。

### 1.4 失败码映射（建议）
| 失败场景 | error_code |
|---|---|
| 检测超时 / 无有效目标（找不到物体） | 3 |
| grasp_result 0/1 重试耗尽（含空抓） | 5 |
| grasp/place give_up 超时（未收到 executor 结果） | 7 |
| home 移动失败 / 其它异常 | 9 |

### 1.5 实现机制
- 集中在 workflow：新增 `_send_report(command, success, code, message)`。
- `_pending_report`（command+code+message）记录任务终态，统一在**确认臂已收回的出口** flush：
  - PICK 成功 → 进 `WAIT_RELEASE_CMD` 的出口；
  - PICK 失败 → `RETURN_HOME` 完成回 `IDLE` 的出口；
  - PLACE 成功/失败 → executor 回 home（`IDLE`）/收臂完成出口。
- STOW：3 s 定时器，条件 `state==IDLE`（是否再加 executor IDLE 见 1.7）。

### 1.6 隐患（本次不实施，仅标注）
1. **90 s 执行超时（重要）**：桥接 PICK/PLACE 只等 90 s；我方最坏路径（检测 15 s + 对准 + ≤3 次重试）可能超过 90 s → 桥接会先自行判 `TIMEOUT(7)`，而臂仍在跑 → 调度失步。**后续需加总时长 watchdog 或临近 90 s 主动发失败上报。**
2. 若我方上报晚于桥接超时，桥接只入 60 s 缓存、调度已判失败，属不可恢复。
3. `grasp_result` 语义改动可能影响 `grasp_only`/RL/测试路径的调用方，需回归确认。
4. 空放路径（空抓→place）改掉后，请确认无人依赖"空爪也走放置"。

### 1.7 待确认
- STOW 门控：**开机时 executor 是否已在 home**？若否，需先收臂再周期发 STOW；workflow `IDLE` 是否足够代表"臂已收回、底盘可动"。
- 是否给上报逻辑加独立参数/开关（便于单测）。

---

## 2. 待改动 B：实机生产启动脚本 + 开机自启动

### 2.1 对照现状 `start_shelf.sh`，生产裁剪
| 组件 | 生产是否要 | 说明 |
|---|---|---|
| CAN 探测/复位 + 自动找臂口 | ✅ | 保留 |
| MoveIt+臂（`auto_home:=true` `use_rviz:=false` `namespace:=arm` …） | ✅ | 保留 |
| RViz（`manual_rviz.launch.py` + `yolo_config_armns.rviz`） | ❌ | **删除** |
| realsense2 camera（`/camera`） | ✅ | 保留 |
| aruco_tracker | ✅ | 保留 |
| yolo_grasp | ✅ | 保留 |
| place_planner | ✅ | 保留 |
| grasp_target_marker（纯 rviz 显示转发） | ❌ | **删除**（无 rviz 无意义） |
| grasp_executor | ✅ | 保留 |
| shelf_workflow | ✅ | 保留 |

- "不传实机不用的额外东西"：脚本本身未传测试参数；`skip_align / preset_home` 是**话题**（测试时手动发），不影响生产，保留即可。

### 2.2 新脚本（建议命名）
- 路径：`/home/s1/tiaozhanbei/agx_arm_ros-ros2/start_arm_prod.sh`
- 内容 = `start_shelf.sh` **去掉 RViz 与 grasp_target_marker** 两段，其余节点、参数、日志（`/tmp/*.log`）一致。
- `/arm_task_report` 由 workflow 内部发布，脚本无需为其加节点。

### 2.3 开机自启动（仿照车侧既有 systemd 模式）
车侧既有模式参考（`/etc/systemd/system/task-scheduler.service`）：
```ini
[Service]
Type=simple
User=s1
ExecStart=/bin/bash <脚本绝对路径>
Restart=always
RestartSec=5
TimeoutStopSec=30
[Install]
WantedBy=multi-user.target
```
- 建议新增 `/etc/systemd/system/agx-arm.service` → `ExecStart=/bin/bash /home/s1/tiaozhanbei/agx_arm_ros-ros2/start_arm_prod.sh`，`systemctl enable agx-arm`。
- 若"机械臂侧"已有既定自启动方式，请指认后按其模式实现。

### 2.4 启动时序协调（隐患）
- 臂自启服务与车侧 `task-scheduler.service` 都开机自启；若调度器先于臂就绪即发 STOW，桥接等 30 s 超时。
- 缓解：臂侧尽早起来并**周期 STOW**（3 s）+ 桥接 60 s 缓存；正式比赛建议**按顺序人工确认启动**或两团队约定服务依赖/顺序。

---

## 3. 建议实施顺序（确认后执行）
1. 改动 A：`shelf_workflow_node` 上报（STOW 周期 / PICK / PLACE / 空抓=失败=5 / `_pending_report` 收臂后 flush）。
2. `start_arm_prod.sh`（裁剪版启动脚本）。
3. `agx-arm.service` + 开机自启。
4. 联调：车侧 `scheduler_mode.conf` 设 `SCHEDULER_USE_STUB=false`，同时跑 `competition.launch.py` 与臂侧全栈验证 STOW/PICK/PLACE 上报闭环。

---

## 4. 待两团队确认清单
1. STOW：开机是否已回 home；`workflow IDLE` 作为"可动"门控是否可接受（改动 A 1.7）。
2. 90 s 超时隐患：本次不做，确认后续排期（改动 A 1.6.1）。
3. 空抓失败码=5（已定）；"找不到物体"走码 3 是否也认可。
4. 生产脚本/systemd 采用上表方案（或按机械臂既有自启动方式）。
5. 启动时序：比赛日是否需要人工按序启动臂→调度器，或加 service 顺序依赖。
